const Expenses = require('@/applications').expenses
const { ErrorHandler } = require("@/utils");

exports.addExpenses = (req,res)=>{
    try {
        const expensesApp = new Expenses()
    } catch (error) {
        
    }
}